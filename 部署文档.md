# CampusLife · Web版部署文档

## 一、项目概述

**CampusLife** 是一个为香港城市大学学生设计的个人助手Web App，集成了：
- 🎓 港城大学习生活（课表、邮件、任务清单）
- 💼 找工作（投递管线、面试、复盘）
- 🏃 减肥生活（饮食拍照记录、运动、体重追踪）
- 📋 总时间表（三模块统一时间轴 + 今日待办）

## 二、技术栈

- **React 18** + **TypeScript**
- **Vite**（构建工具）
- **React Router**（路由）
- **Lucide React**（图标）
- **date-fns**（日期处理）

## 三、本地运行

### 前提条件
- Node.js 18+
- npm 或 pnpm

### 步骤

```bash
# 1. 进入项目目录
cd /workspace/CampusLife

# 2. 安装依赖（如果还没装）
npm install

# 3. 启动开发服务器
npm run dev

# 4. 浏览器打开 http://localhost:5173
```

## 四、部署到公网（推荐 Vercel）

### 方式一：Vercel（最简单）

1. 把代码推送到 GitHub
   ```bash
   # 在 GitHub 创建仓库，然后
   git init
   git add .
   git commit -m "init"
   git remote add origin https://github.com/你的用户名/campuslife.git
   git push -u origin main
   ```

2. 去 [vercel.com](https://vercel.com) 注册账号
3. 点击 "Add New Project"
4. 选择你的 GitHub 仓库
5. 点击 Deploy，等1分钟，获得公网网址
6. 网址格式：`https://campuslife-xxx.vercel.app`

### 方式二：Netlify

类似Vercel，去 [netlify.com](https://netlify.com) 拖拽 `dist` 文件夹即可。

### 方式三：静态文件直接部署

```bash
# 1. 构建生产包
cd /workspace/CampusLife
npm run build

# 2. dist/ 目录就是完整的静态网站
# 3. 把 dist/ 目录上传到任何静态托管服务即可
```

## 五、iPhone 添加到主屏幕（像原生App一样用）

### 步骤

1. **用 Safari 打开你的网址**（如 `https://campuslife-xxx.vercel.app`）
2. 点击底部中间的 **分享按钮**（方框带向上箭头）
3. 向上滑动，找到 **"添加到主屏幕"**
4. 点击 **"添加"**
5. 手机主屏幕上会出现 **CampusLife** 图标
6. 点击图标打开，**没有Safari地址栏**，体验像原生App

### 效果
- 全屏显示，没有浏览器地址栏
- 支持离线缓存（后续可加Service Worker）
- 底部安全区域适配（iPhone底部手势条）
- 图标和启动画面已配置

## 六、数据存储说明

当前版本使用 **内存中的 Mock 数据**，刷新页面数据会丢失。

### 后续升级方案

| 方案 | 说明 | 复杂度 |
|------|------|--------|
| localStorage | 数据存在浏览器本地，刷新不丢 | 低 |
| IndexedDB | 更强大的本地存储，支持图片 | 中 |
| 后端API | 数据存云端，多设备同步 | 高 |

建议先用 localStorage，后续需要多设备同步再加后端。

## 七、后续开发方向

### 已完成功能
- ✅ 首页总时间表 + 今日待办
- ✅ 港城大：课表（周/日视图）、邮件分类、任务清单
- ✅ 找工作：双轨投递管线、面试日历、复盘日记
- ✅ 减肥：饮食记录、运动记录、体重趋势图
- ✅ 底部Tab导航
- ✅ 移动端适配 + PWA支持

### 待开发功能
- [ ] 163邮箱IMAP接入（真实邮件拉取）
- [ ] 课程表导入（.ics文件或手动模板）
- [ ] 拍照识别食物（调用AI API）
- [ ] 数据持久化（localStorage）
- [ ] 推送通知（课程提醒、面试提醒）
- [ ] 冲突预警引擎（时间冲突检测）
- [ ] 校园地图、校历信息
- [ ] GPA追踪计算

## 八、项目文件结构

```
CampusLife/
├── index.html              # HTML入口
├── src/
│   ├── main.tsx            # React入口
│   ├── App.tsx             # 路由配置
│   ├── index.css           # 全局样式
│   ├── types/
│   │   └── index.ts        # TypeScript类型定义
│   ├── mock/
│   │   └── data.ts         # Mock数据
│   ├── components/
│   │   ├── BottomNav.tsx   # 底部导航
│   │   └── ModuleTag.tsx   # 模块标签
│   └── pages/
│       ├── Home.tsx        # 首页（总时间表）
│       ├── Campus.tsx      # 港城大模块
│       ├── Job.tsx         # 找工作模块
│       ├── Fitness.tsx     # 减肥模块
│       └── Profile.tsx     # 我的页面
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## 九、常见问题

**Q: 为什么截图颜色很淡？**
A: 这是Chromium headless模式的渲染问题，实际在手机上用Safari打开颜色会正常。

**Q: 数据刷新后会丢失吗？**
A: 是的，当前是Mock数据。后续我会加localStorage持久化。

**Q: 可以离线使用吗？**
A: 当前需要联网访问网址。后续加Service Worker后可离线使用。

**Q: 能推送到手机通知吗？**
A: Web版推送需要用户授权，且iOS Safari对Web推送支持有限。后续转原生App后推送体验更好。

---

**项目位置**: `/workspace/CampusLife/`

**构建产物**: `/workspace/CampusLife/dist/`
